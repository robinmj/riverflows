
apply plugin: 'java'

sourceSets {
    test {
        java {
            srcDir 'src'
        }
    }
}

dependencies {
    testCompile fileTree(dir: 'lib', include: '*.jar')
    testCompile project(':Datasources')
}
