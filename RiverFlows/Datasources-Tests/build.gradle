
apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}

dependencies {
    compile fileTree(dir: 'lib', include: '*.jar')
    compile project(':Datasources')

    compile "junit:junit:4.11"
}
